/*
import React from "react"
import Item from "./Item";

class List extends React.Component {

//take the prop.todos shared by the Container component, maps it, storages it and shared it
  render() {
    return (
      <ul>
        {this.props.menu.map(myitem => (
          <Item key={myitem.id} myitem={myitem} />
        ))}
      </ul>
    )
  }
}

export default List;
*/