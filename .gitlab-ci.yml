stages:
- build
- test 

build:
  stage: build
  script:
    - cd S3_IP_Muziekzaals
    - ./gradlew build -x test
    - ./gradlew.bat --stop 
test:
  stage: test
  script:
    - cd S3_IP_Muziekzaals
    - ./gradlew test
    - ./gradlew.bat --stop
