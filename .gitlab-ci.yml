stages:
- build
- test 

build:
  stage: build
  script:
    - cd s3_gps_ivanti
    - ./gradlew build -x test
    - ./gradlew.bat --stop 
test:
  stage: test
  script:
    - cd s3_gps_ivanti
    - ./gradlew test
    - ./gradlew.bat --stop
